# Copy to .env (gitignored) — NEVER commit the real key.
# Used by: npm run auth-server
RESEND_API_KEY=re_your_key_here
# Optional once you verify a domain in Resend:
# RESEND_FROM="SendPlate <verify@yourdomain.com>"
